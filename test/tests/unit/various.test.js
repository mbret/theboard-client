var request = require('supertest');
var assert = require("assert");
//var should = require("should");

describe('Various', function() {

    describe("Request User", function(){
        it('should implement minimal method as guest', function( done ){
            // @todo find a way to test req.user somewhere
            done();
        })
    });

});