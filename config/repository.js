/**
 *
 * @type {{copy: string, pageTitle: string, environment: (*|string), toastr: {positionClass: string}}}
 */
module.exports.repository = {
    
    localPath: require('path').join(process.cwd(), 'data/statics/repository'),
    localUri: '/repository',

    remoteUrl: '...'

};